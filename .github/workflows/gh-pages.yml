name: github pages

on:
  push:
    branches:
      - gh-pages-preview  # Branch that drives the Pages preview at https://predictiple.github.io/velociraptor-docs/

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true  # Fetch Hugo themes (true OR recursive)
          fetch-depth: 0    # Fetch all history for .GitInfo and .Lastmod

      - uses: actions/setup-go@v5
        with:
          go-version: '^1.25'

          # Caching seems to really slow down the build due to the time
          # taken to save the cache.
          cache: false

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: 'latest'
          # extended: true

      - name: Prepare
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python scripts/exchange_index.py
          python scripts/exchange_verify.py
          python scripts/blog_index.py
          python scripts/knowledge_base.py

      - name: BuildIndex
        run: make index

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Build
        # Use configure-pages' base_url output so all asset paths resolve
        # under the repo subpath (e.g. /velociraptor-docs/css/...). This is
        # the official Hugo-on-GitHub-Pages pattern and removes the need for
        # any relURL/siteBase hacks in templates or assets.
        run: hugo --baseURL "${{ steps.pages.outputs.base_url }}/"

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: BuildSearchIndex
        run: bun run pagefind

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4